[project]
name = "quickshell-preview"
version = "0.1.0"
description = "Local Qt runner for shared Quickshell preview views"
requires-python = ">=3.10"
dependencies = ["PySide6==6.8.3"]

[tool.uv]
package = false